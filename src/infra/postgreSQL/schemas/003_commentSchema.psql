DROP IF EXISTS comment;

CREATE TABLE comment(
  id_comment INT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  id_author INT,
  FOREIGN KEY(id_author)
    REFERENCES users(id_user)
      ON UPDATE CASCADE,
  id_post INT,
  FOREIGN KEY(id_post)
    REFERENCES posts(id_post)
)